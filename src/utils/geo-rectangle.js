"use strict"

/**
 * Rectangle calculations
 * @memberof utils.geometry
 * @namespace rectangle
 */

const geoRectangle = ({ lib, swLib }) => {

    const getControlPoints = (length, width) => {
        return null;
    }

    const centre = (length, width) => {
        return null;
    }

    const diagonalLength = (length, width) => {
        return null;
    }

    return {
        getControlPoints,
        centre,
        diagonalLength,
    }
}

module.exports = { init: geoRectangle };
